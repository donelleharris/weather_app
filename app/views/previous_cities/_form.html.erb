<%= form_with(model: previous_city, local: true) do |form| %>
  <% if previous_city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(previous_city.errors.count, "error") %> prohibited this previous_city from being saved:</h2>

      <ul>
      <% previous_city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city, id: :previous_city_city %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
